import { MdOutlineRemoveRedEye } from "react-icons/md";

const PreviewButton = () => {
  const handlePreviewButtonClick = () => {};
  return (
    <button onClick={handlePreviewButtonClick} className="tool_btn">
      <MdOutlineRemoveRedEye />
    </button>
  );
};

export default PreviewButton;
